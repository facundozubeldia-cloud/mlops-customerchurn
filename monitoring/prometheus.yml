global:
  scrape_interval: 15s  # cada 15 segundos recolecta métricas

scrape_configs:
  - job_name: "andeslink-api"
    static_configs:
      - targets: ["api:8000"]  # nombre del servicio en Docker Compose